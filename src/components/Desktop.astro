---
/** Desktop: Fixed desktop environment background */
---

<div class="desktop-background" data-desktop-canvas>
	<slot />
</div>

<style is:inline>
	.desktop-background {
		position: fixed;
		top: var(--menubar-height);
		left: 0;
		right: 0;
		bottom: 0;
		background: var(--gray-10);
		overflow: hidden;
		/* Mac desktop pattern */
		background-image: radial-gradient(
			circle at 2px 2px,
			rgba(0, 0, 0, 0.1) 1px,
			transparent 0
		);
		background-size: 20px 20px;
		/* IMPORTANTE: permitir eventos para que las ventanas (hijas) puedan recibir drag */
		pointer-events: auto;
		user-select: none;
		-webkit-user-drag: none;
		z-index: 1;
	}

	/* Windows should be above the background and fully interactive */
	/* .desktop-background .draggable-window {
		pointer-events: all;
		z-index: 10;
	}

	/* Ensure no draggable classes are applied to desktop */
	/* .desktop-background:not(.draggable-window) {
		-webkit-user-drag: none !important;
		-moz-user-select: none !important;
		-webkit-user-select: none !important;
		user-select: none !important;
	} */ */
</style>
